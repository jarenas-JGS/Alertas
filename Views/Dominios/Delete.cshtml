@model Alertas.Models.Dominio

@{
    ViewData["Title"] = "Eliminar dominio";
    bool tieneRelaciones = ViewBag.TieneRelaciones ?? false;
}

<div class="container mt-4">

    <h3>Eliminar dominio</h3>

    <div class="card shadow-sm">
        <div class="card-body">

            @if (tieneRelaciones)
            {
                <div class="alert alert-warning">
                    Este dominio no puede eliminarse porque tiene registros asociados.
                </div>
            }
            else
            {
                <div class="alert alert-danger">
                    ¿Estás seguro de que deseas eliminar este dominio?
                </div>
            }

            <dl class="row">
                <dt class="col-sm-3">Id</dt>
                <dd class="col-sm-9">@Model.id_dominio</dd>

                <dt class="col-sm-3">Nombre</dt>
                <dd class="col-sm-9">@Model.nombre</dd>
            </dl>

            <form asp-action="Delete" method="post">
                @Html.AntiForgeryToken()

                <input type="hidden" asp-for="id_dominio" />

                <div class="d-flex justify-content-end gap-2">
                    <a asp-action="Index" class="btn btn-outline-secondary">Volver</a>

                    @if (!tieneRelaciones)
                    {
                        <button type="submit" class="btn btn-danger">
                            Eliminar
                        </button>
                    }
                </div>
            </form>
        </div>
    </div>
</div>