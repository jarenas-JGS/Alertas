@{
    ViewData["Title"] = "Cargue de Obligaciones por Plantilla";
}

<div class="container py-4">
    <div class="card shadow-sm border-0">
        <div class="card-body p-4">
            <h2 class="fw-bold mb-2">Cargue de Obligaciones por Plantilla</h2>
            <p class="text-muted mb-4">
                Este módulo se encuentra en construcción.
            </p>

            <div class="alert alert-info mb-0">
                Aquí se desarrollará el cargue masivo de obligaciones mediante archivo Excel.
            </div>
        </div>
    </div>
</div>